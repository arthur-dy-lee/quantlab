"""Streamlit 本地仪表盘（详细设计 §10.3）。M2。

运行：`streamlit run quantlab/dashboard/app.py`（需 `pip install -e '.[dashboard]'`）。
调用同一功能层 + viz。TODO(M2)。
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("TODO(M2): dashboard")


if __name__ == "__main__":
    main()
